[package]
name = "restrict_lang"
version = "0.1.0"
entry = "src/main.rl"
edition = "2025"

[dependencies]
# Standard library (built-in)
# std = { path = "./std", version = "0.1.0" }

# External dependencies would go here
# web-utils = "1.2.*"                         # Cage from WardHub
# local-lib = { path = "../local-lib" }       # local cage
# foreign-wit = { wasm = "https://host/math.wasm", wit = "https://host/math.wit" }

[build]
target = "wasm32"     # wasm32 | native-x86_64 | host
optimize = true
output = "dist/"